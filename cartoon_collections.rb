def greet_characters(array)
  array.each do |guy|
    puts "Hello #{guy}!"
  end 
end

def list_dwarves(array)
  array.each_with_index do |guy|
    puts guy.index += 
  end 
  
  # Use `each_with_index` to enumerate over the provided array
  #
  # Print a numbered list of each element
end